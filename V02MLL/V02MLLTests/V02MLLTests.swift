import XCTest
@testable import My_little_Life

class V02MLLTests: XCTestCase {

    override func setUpWithError() throws {
        // Код, который выполняется перед началом каждого теста
    }

    override func tearDownWithError() throws {
        // Код, который выполняется после завершения каждого теста
    }

    func testExample() throws {
        // Пример функционального теста
        // Используйте XCTAssert и связанные функции для проверки результатов теста
    }

    func testPerformanceExample() throws {
        // Пример теста производительности
        measure {
            // Код, время выполнения которого нужно замерить
        }
    }
}
